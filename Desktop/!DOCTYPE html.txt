<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Feliz aniversario mi niño tititito 💜</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffb6c1, #ff69b4);
      overflow: hidden;
      font-family: 'Comic Sans MS', cursive;
      color: white;
      text-align: center;
    }

    h1 {
      margin-top: 50px;
      font-size: 2.5em;
      text-shadow: 2px 2px 10px #ff0080;
    }

    .gallery {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      margin: 50px auto;
      width: 90%;
      max-width: 800px;
    }

    .gallery img { src=
      width: 200px;
      height: 200px;
      object-fit: cover;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(255,255,255,0.8);
      transition: transform 0.3s;
    }

    .gallery img:hover {
      transform: scale(1.1);
    }

    .mensaje {
      font-size: 1.3em;
      margin: 20px;
      text-shadow: 1px 1px 8px #ff1493;
    }

    /* corazones animados */
    .heart {
      position: absolute;
      color: #ff0055;
      font-size: 20px;
      animation: flotar